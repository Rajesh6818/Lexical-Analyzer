# Lexical-Analyzer
lexical analyzer in C that reads source code and breaks it into meaningful tokens such as keywords, identifiers, operators, constants, and special symbols. The project demonstrates a fundamental phase of compiler design by performing lexical analysis and basic error detection.
